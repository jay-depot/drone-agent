---
key: plan-inventory-code-verification-2026-09-18
tags:
  - audit
  - plan
  - verification
created: 2026-09-19T00:40:49.743Z
updated: 2026-09-19T00:40:49.743Z
---

Plan-vs-code verification of all 19 plan documents (2026-09-18, branch feat/coordinator-config-ui-and-secure-storage). Method: read each plan's expected artifacts, then grep/read the code + cross-check the commits the plan records against `git log`.

DONE (15): plan-integration-test-beacon-isolation (shouldSkipIntegrationSuite in 6 suites + subagent/dispatch, test-runner Dockerfile, test:integration orchestration); plan-lsp-symbolic-resolution-improvements (surroundingText in navigation.ts+completion.ts POSITION_PROPERTIES, position.ts matchesSurroundingBlock anchor filter, auto-expansion, reference-cache.ts + editing.ts referenceId handshake); plan-drone-swarm-pipeline-infra (drone-swarm pkg w/ address/client/index, drone-swarm-common config-file.ts loadConfigFile+mergeConfig, --config-file both servers, session-end.ts both layers, outbox table+db/outbox.ts+outbox-flusher.ts, docs/agents/memory-pipeline.md); plan-coordinator-trust-hardening (db/init.ts:218 fingerprint_confirmed_at migration, ip.ts isLoopbackIp, confirm-fingerprint route + mtls exemption, UI banners; commit bf3a7ef); plan-coordinator-config-ui-and-secret-handling (drone-core config-keys.ts KNOWN_CONFIG_KEYS+UNDERLAY_ALLOWLIST+isUnderlayAllowed, coordinator routes/config.ts, UI config.tsx, rebuild() wiring; commits 9a24458..d4527ac); plan-coordinator-stored-secrets-and-config-ux (coordinator_secrets table, db/secrets.ts, config-resolve.ts buildDistributionEntries/resolveSecretRefs, beacon secret-overlay.ts, stored-secrets-modal.tsx; commits 00700e2..d081459); plan-receiver-side-env-var-interpolation (resolveEnvTemplates in swarm/config.ts; commit fcf246a1); plan-swarm-session-import-provider-fixes (SwarmPluginDeps.resolveContextWindow, createSwarmPlugin(config,deps?), registerDriver mocks; commits 50e31d1/10484f8); plan-compaction-oldest-turns-helper-consolidation (getOldestNonSummaryTurns in turn-utils.ts used by session-manager+context-budget-service+compaction; dropOldestTurns gone); slash-commands-during-work-fix (busyBehavior in drone-core plugin-system.ts:495, --now strip in slash-parse.ts, app.tsx dispatch; commit 3df9b0e); fix-swarm-config-underlay-rebuild (normalizeFlatUnderlay + maskScalar whole-template pass-through + rebuild precedence; commit feb18d2); fix-swarm-wiki-write-error-masking (proxyToCoordinatorDetailed/readErrorBody/CoordinatorProxyResult in beacon routes/context.ts; commit 849f2c8); conversation-assembly-cleanup (renderPromptFragmentsByPhase('header') in skills-plugin.test.ts; plugin-engine.ts buildSystemMessages optional w/ single JSDoc; commit 489dc88); image-content-refactor-v2 (DroneToolResult, splitToolResultBlocks, maxImagesPerMessage; commit 9ddbe31); seed-receiver-side-env-var-interpolation (CONSUMED).

NOT DONE (2): (1) plan-compaction-buildfragment-required — untouched; CompactionPluginDeps still `buildFragmentMessages?:` optional (index.ts:578) with "Defaults to returning an empty array" JSDoc, and registration still `deps.buildFragmentMessages ?? (async () => [])` (index.ts:616). Note the OTHER type (RegistrationContext, index.ts:36) IS required — the plan targeted CompactionPluginDeps. Body says "Ready for execution". (2) plan-swarm-remote-spawn-lifecycle — only fragments landed: swarm_spawn exists but has NO `mode: sync|async` input (targetBeaconId/personaId/task/config/spawnId only), no spawnResult WS push, no pending-result registry in swarm ctx, no `swarmSpawned` flag in spawner argv. The spawnRoots advertise-and-enforce + `--output-json` + `--swarm/--session-id/--beacon-host` bits came from ADR 197/198, not this plan.

DONE BY A DIFFERENT ROUTE (1): plan-codeql-uncontrolled-data-path-expression. Spawner half matches the plan exactly (MAX_WORKING_DIR_LENGTH=4096 at spawner.ts:52, throw at 98-101, new spawner.test.ts). Wiki half did NOT get the plan's `pagePath` length guard (no pageId.length / MAX_PAGE_ID_LENGTH anywhere) and no over-length page-ID tests; instead the 4 wiki sinks are cleared by `// codeql[js/path-injection]` in-source dismissals (wiki-storage.ts:156/194/231/243/271) enabled by ADR 171's AlertSuppression query pack, plus a MAX_WIKI_CONTENT_LENGTH=1e6 regex guard (commit bc8f393, PR #60). Net effect = CodeQL green, but step 1 of the plan is not implemented as written.

INTENTIONALLY NOT DONE (1): mcp-resource-block-future-plan — explicitly a "future plan, NOT in V2". splitToolResultBlocks still drops resource blocks with a comment saying so; that is the documented V2 constraint, not a gap.

Consequence: `done` cannot be inferred from file name, `plan` tag, or body prose — only plan-integration-test-beacon-isolation has a machine-readable `status:` field. A future audit should treat plan-compaction-buildfragment-required as the only live un-started plan, and plan-swarm-remote-spawn-lifecycle as partially landed.
