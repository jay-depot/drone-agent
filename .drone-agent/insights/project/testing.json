[
  {
    "timestamp": "2026-06-29T03:47:20.903Z",
    "insight": "The beacon's coordinator-independent operation was verified via live endpoint testing (starting the beacon with no coordinator and hitting all endpoints with curl). This is effective but manual — adding automated integration tests that start the beacon and test endpoints without a coordinator would prevent regressions."
  },
  {
    "timestamp": "2026-06-29T05:47:57.067Z",
    "insight": "The external loader tests revealed that `os.homedir()` reads from the OS user database, not `process.env.HOME`. Tests that need to override the home directory for user-scoped paths must set `process.env.HOME` before calling functions that use `os.homedir()`. This is a subtle gotcha that could trip up future test authors."
  },
  {
    "timestamp": "2026-07-01T23:25:09.699Z",
    "insight": "The file.test.ts has 28 tests for applyPatch in isolation but zero integration tests for the full file__apply_diff tool execution path (schema parsing → applyPatch → JSON response → TUI rendering). This allowed bugs in the glue code (patched vs written field name, ANSI codes in LLM context) to go undetected."
  },
  {
    "timestamp": "2026-07-06T00:25:41.214Z",
    "insight": "The vitest alias configuration in vitest.config.ts resolves 'drone-swarm-common/wiki-storage' and 'drone-swarm-common/tls' to source files, but these aliases don't work for dynamic import() calls from test files. This means route tests that use dynamic imports (like the wiki routes) can't easily set up the wiki directory before the routes are registered. A workaround is to use the dist file path directly, but that creates a different module instance than the route files use."
  },
  {
    "timestamp": "2026-07-06T00:25:41.221Z",
    "insight": "The coordinator and beacon index.ts files have top-level imports of TLS, spawner, and WebSocket modules that don't resolve in the test environment. To test routes, we need app-helper.ts files that build a minimal Fastify instance with just route registrations, avoiding the full index.ts import chain."
  },
  {
    "timestamp": "2026-07-06T00:36:44.771Z",
    "insight": "The plan-coordinator-route-tests plan specified per-route test files (e.g., test/routes/health.test.ts, test/routes/personas.test.ts), but I used a single monolithic file (test/routes.test.ts). The monolithic approach is simpler and avoids duplication of the beforeEach/afterEach setup, but the per-route approach would be better for CI parallelism and readability. For now the monolithic file is fine since vitest uses singleFork anyway."
  },
  {
    "timestamp": "2026-07-06T00:36:44.775Z",
    "insight": "The plan specified a shared harness (makeApp) that sets up DB+storage+wiki in one call using buildApp() from index.ts. However, buildApp() imports drone-swarm-common/tls which doesn't resolve in the test environment. I created a test-specific app-helper.ts instead. The helpers/server.ts file exists as a reference but can't be used until the TLS import issue is resolved."
  },
  {
    "timestamp": "2026-07-06T00:36:44.784Z",
    "insight": "Wiki route tests were deferred because the route files use dynamic import('drone-swarm-common/wiki-storage') which goes through vitest's alias, but dynamic imports from test files don't go through the alias. This means setKnowledgeBaseDir() called from a test file uses a different module instance than the route files. The wiki storage is tested separately in wiki-storage.test.ts."
  },
  {
    "timestamp": "2026-07-06T00:43:08.979Z",
    "insight": "The DroneToolDescriptor type (used by LLM provider chat() methods) does not have an 'execute' property — that's on DroneToolDefinition (used by the plugin engine). Several test files were passing 'execute' in tool objects to chat(), which caused TS errors. The fix was to remove the 'execute' property since it's not needed by the LLM provider."
  },
  {
    "timestamp": "2026-07-06T00:43:08.983Z",
    "insight": "The DroneAgentConfig type requires 'openai' and 'anthropic' fields (not optional), but several test files were constructing partial config objects without them. The fix was to add minimal openai/anthropic config stubs. This suggests the config type might benefit from a PartialDroneAgentConfig-based helper or a test fixture factory."
  }
]
