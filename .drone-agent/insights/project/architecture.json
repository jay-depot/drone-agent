[
  {
    "timestamp": "2026-06-22T06:18:11.008Z",
    "insight": "The skills plugin lacks a reload mechanism entirely — skills are loaded once in onPluginsLoaded. The persona plugin has reloadPersonas() in its capability. A skills.create workflow will need a reloadSkills() capability and a skills.reload tool so the assistant can trigger a reload after writing the skill body."
  },
  {
    "timestamp": "2026-06-22T18:15:49.149Z",
    "insight": "The persona directory structure change is a significant refactor that touches persona loading, skill loading (persona-owned skills), insight storage, the persona wizard, and all tests. The current flat `personas/<name>.md` layout needs to become `personas/<name>/persona.md` with subdirectories for skills and insights."
  },
  {
    "timestamp": "2026-06-22T19:56:50.190Z",
    "insight": "The config merge in `applyAgentConfigLayer` uses `??` (replace) for all fields. The prompt-file plugin needs additive (concatenate) merging for its `files` array across config layers. This requires a new merge pattern in `applyAgentConfigLayer`."
  },
  {
    "timestamp": "2026-06-22T21:58:00.060Z",
    "insight": "The config system currently has no write path — config is loaded at startup and is read-only at runtime. A config plugin needs to bridge this gap by writing back to the config files on disk. The plugin will need access to the config layers (file paths) which are currently only available in main() before the engine is created."
  },
  {
    "timestamp": "2026-06-22T22:10:02.396Z",
    "insight": "The config plugin re-discovers layers from disk independently of the engine's config, which is necessary because the engine's config is set at init time and doesn't reflect disk changes. However, this means config.get and registration.getConfig() can disagree if the engine was initialized with a different config than what's on disk. The plugin handles this by merging discovered layers itself for config.get."
  },
  {
    "timestamp": "2026-06-22T22:46:13.707Z",
    "insight": "The conversation service and CLI entry point hardcode `engine.getCapability<OllamaCapability>('ollama')` to resolve the LLM provider. This makes it difficult to add alternative providers like OpenRouter. A broker pattern (like skills/personas) would decouple provider resolution from the consumer."
  },
  {
    "timestamp": "2026-06-23T00:01:35.192Z",
    "insight": "The LSP diagnostics show two test files (log-plugin.test.ts and prompt-file.test.ts) have type errors because the DroneAgentConfig type now requires `llm` and `openrouter` fields that the test fixtures don't provide. These are pre-existing issues unrelated to this feature."
  },
  {
    "timestamp": "2026-06-23T02:36:11.560Z",
    "insight": "The config.ts file had massive duplication in validation logic (~500 lines of near-identical type checks). Extracting reusable validation helpers (validateString, validateBoolean, validatePositiveNumber, etc.) reduced the file by ~14% and made the validation patterns consistent. The same pattern of duplication exists in other parts of the codebase (e.g., the 8 copies of isRecord) — a shared type-guards module was created to address this."
  },
  {
    "timestamp": "2026-06-23T02:41:55.164Z",
    "insight": "The config validation in config.ts is ~700 lines of repetitive boilerplate that could be replaced with a schema library. This is a high-impact refactor because every new config field requires manual validation code, and the hand-rolled validators are easy to get wrong (e.g., the `validatePercent` function rejects 0 but the compaction `softThresholdPercent` might legitimately be 0 to disable it)."
  },
  {
    "timestamp": "2026-06-23T02:41:55.174Z",
    "insight": "The app.tsx TUI component is 834 lines with 15+ useState/useRef/useCallback/useEffect calls at the top level. This is a god component that mixes state management, event handling, slash command dispatch, and rendering. Extracting custom hooks would make the code more testable and maintainable."
  },
  {
    "timestamp": "2026-06-23T02:41:55.179Z",
    "insight": "The search.ts plugin uses execSync (synchronous child process) inside an async execute function, blocking the event loop. It also constructs shell command strings with a fragile quoteArg function instead of using args arrays like git.ts does correctly."
  },
  {
    "timestamp": "2026-06-23T02:41:55.183Z",
    "insight": "The DroneTuiOptions type in types.ts manually re-declares a subset of the engine interface instead of using Pick<DronePluginEngine, ...>. This will drift as the engine interface evolves."
  }
]