# SKILL LIBRARY

This folder, which isn't treated as a package in the monorepo, contains a library of skills I've found useful when using drone-agent. These skills are not part of the core drone-agent package, but they can be copied into `~/.drone-agent/skills/` to extend the agent's capabilities. You can probably also just point your assistant at the URL for one, and ask it to import the skill for you.
