# CodeQL advanced configuration with suppression-comment support (dismiss-alerts).
# Re-trigger: verify advanced config uploads SARIF after disabling default setup.
name: 'CodeQL Advanced'

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  schedule:
    - cron: '31 7 * * 3'

jobs:
  analyze:
    name: Analyze (${{ matrix.language }})
    runs-on: ubuntu-latest
    permissions:
      security-events: write
      packages: read
      actions: read
      contents: read

    strategy:
      fail-fast: false
      matrix:
        include:
          - language: actions
            build-mode: none
          - language: javascript-typescript
            build-mode: none
            query: codeql/javascript-queries:AlertSuppression.ql
            # The analyze action names its SARIF output after the canonical
            # CodeQL language (javascript), not the workflow alias.
            sarif-name: javascript

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Initialize CodeQL
        uses: github/codeql-action/init@v3
        with:
          languages: ${{ matrix.language }}
          build-mode: ${{ matrix.build-mode }}
          packs: ${{ matrix.query }}

      - name: Perform CodeQL Analysis
        id: analyze
        uses: github/codeql-action/analyze@v3
        with:
          category: '/language:${{ matrix.language }}'
          output: sarif-results

      - name: Dismiss alerts
        # dismiss-alerts only works on the default branch (main); on PRs the
        # alerts are shown as warnings but the job stays green (neutral).
        if: github.ref == 'refs/heads/main'
        uses: advanced-security/dismiss-alerts@v2
        with:
          sarif-id: ${{ steps.analyze.outputs.sarif-id }}
          sarif-file: sarif-results/${{ matrix.sarif-name || matrix.language }}.sarif
        env:
          GITHUB_TOKEN: ${{ github.token }}
